<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.k.sorukaydedici">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ana_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ana_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NasilActivity"></activity>
        <activity android:name=".SkorActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".SorularActivity" />
        <activity android:name=".KlasorActivity" />
        <activity android:name=".FavoriActivity" />
        <activity android:name=".SoruActivity" />
        <activity android:name=".EkleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>